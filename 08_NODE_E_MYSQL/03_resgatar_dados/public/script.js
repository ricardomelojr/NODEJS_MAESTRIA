// Seleciona o botão pelo ID
const fazOLButton = document.getElementById('fazOLButton');

// Adiciona um event listener para o evento de clique
fazOLButton.addEventListener('click', () => {
  // Redireciona para a outra página
  window.location.href = '/books';
});
